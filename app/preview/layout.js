export const metadata = {
  title: "Instant Product Demo",
  description:
    "Explore NovaFlow instantly with fictional evaluation data and no account required.",
  robots: { index: false, follow: false },
};

export default function PreviewLayout({ children }) {
  return children;
}
